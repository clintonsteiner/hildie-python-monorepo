[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "archive-git-forks"
version = "0.1.0"
description = "Archive GitHub forked repositories locally and make them private"
readme = "README.md"
requires-python = ">=3.11"
authors = [{name = "Developer", email = "dev@example.com"}]
license = {text = "MIT"}
dependencies = [
    "click>=8.1.0",
    "requests>=2.31.0",
]

[project.scripts]
archive-git-forks = "archive_git_forks.main:cli"

[project.urls]
Homepage = "https://github.com/example/python-monorepo"
Repository = "https://github.com/example/python-monorepo"

[tool.hatch.build.targets.wheel]
packages = ["src/archive_git_forks"]

[dependency-groups]
dev = [
    "pytest>=7.4.0",
    "pytest-cov>=4.1.0",
    "click>=8.1.0",
]
